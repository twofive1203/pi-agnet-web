# Tray assets

Place `tray-icon.png` (16/32px) here for the Windows tray icon.

The main process falls back to a 1×1 placeholder when the file is missing so packaging smoke can run without art finals.
