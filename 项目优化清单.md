# 项目优化清单

> 蜗牛派（Snail Pi Web）产品质量体检与优化建议  
> 性质：基于当前代码、配置和项目文档的静态分析  
> 原则：功能已基本稳定，优先修复明确风险和核心链路回归缺口，不做无指标的大规模重构

---

## 1. 阅读说明

本文将条目分为三类，避免把产品建议误写成已证实缺陷：

| 标记 | 含义 |
| --- | --- |
| **已确认** | 当前代码或文档能够直接证明的问题或约束 |
| **待量化** | 存在合理风险，但需要性能数据、故障记录或浏览器验证才能决定方案 |
| **产品建议** | 主要属于信息架构、交互或部署体验取舍，不代表当前实现错误 |

优先级定义：

| 优先级 | 定义 |
| --- | --- |
| **P0** | 存在明确安全、数据损坏或核心功能不可用风险，应优先处理 |
| **P1** | 高收益的稳定性、回归防护或主路径体验改进 |
| **P2** | 有价值但不阻塞当前发布，可按维护节奏处理 |

---

## 2. 总体判断

| 维度 | 当前判断 | 关键依据 |
| --- | --- | --- |
| 安全模型 | 整体较强；上传路径边界已在第一阶段收口 | 默认 loopback、Server Access Key、HTTPS 默认门、Exact Origin、Automation/Browser 独立 loopback 门；上传经 basename 净化 + 包含校验 + 独占写入 |
| 核心会话链路 | 功能成熟；主聊天生命周期 integration smoke 已补齐 | SSE、retry、`agent_settled`、extension UI、session index 已完善；`test:agent-stream` 含 throttler + empty-completed + chat lifecycle harness |
| 产品认知负担 | 偏高 | Fork、Branch、WorkTree、SnFlow、Automation、Browser Binding 概念并存 |
| 配置体验 | 功能完整但入口较重 | Models、Settings、MCP、Agents 能力密集，常用项与高级项仍可进一步分层 |
| 测试策略 | 领域 smoke 强，交互问题以人工反馈为主 | 不引入需要安装浏览器的 UI E2E；优先保证编译、类型检查和 smoke 的执行速度 |
| 性能与可维护性 | 存在明确热点，优化应先量化 | Usage/文件根仍有全量会话扫描；长会话完整渲染；若干中枢文件超过 1.5k 行 |
| 运维边界 | 官方路径安全，非官方多实例配置不受支持 | 官方 launcher 与 PM2 配置均为单实例；普通聊天会话状态仍是进程内模型 |

---

# 3. P0：立即处理

## 3.1 上传文件名可能逃出上传目录

**状态：已修复（第一阶段完成）**

实现要点：`lib/file-upload.ts` 将 multipart 文件名收敛为 basename 并过滤控制字符/Windows 保留名；`resolve` + 目录包含校验；`wx` 独占创建；上传根尊重 `PI_CODING_AGENT_DIR`；回归见 `npm run test:file-upload`。

### 当前实现

`app/api/files/upload/route.ts` 直接使用 multipart 中的 `File.name` 拼接目标路径：

```ts
const originalName = file.name;
let targetPath = path.join(targetDir, originalName);
fs.writeFileSync(targetPath, buffer);
```

当前没有在写入前执行：

- `path.basename()` 收敛；
- `path.resolve()` 后的目录包含校验；
- `..`、绝对路径、控制字符和 Windows 特殊文件名处理。

Node 的 multipart `File.name` 可以保留 `../../models.json` 一类值。在 Windows 上，`path.join(targetDir, "../../models.json")` 会解析到上传目录之外。

### 影响

攻击者或异常客户端可能在 Node 进程可写范围内创建上传目录之外的新文件。是否能够覆盖既有文件还受当前“重名自动改名”逻辑影响，但**路径边界被突破本身已经成立**。

Server 模式的全局认证和 Origin 门能够降低远程利用面，但不能替代上传路由自身的路径包含约束。本地模式下 `proxy.ts` 会直接放行，建议同时对跨 Origin 本地请求做专项回归验证。

### 建议方案

1. 将展示名和存储名分离；存储名优先使用服务端生成的随机 ID。
2. 如需保留原始文件名，至少使用 `path.basename()` 并过滤：
   - `.`、`..`；
   - `/`、`\`；
   - NUL/控制字符；
   - Windows 保留名和尾部点/空格。
3. 写入前执行最终包含校验：
   - `targetPath = path.resolve(targetDir, safeName)`；
   - 验证 `targetPath` 严格位于 `targetDir` 内。
4. 使用独占创建或明确的覆盖策略，避免检查与写入之间的竞态。
5. 增加回归用例：
   - `../../x`；
   - `..\..\x`；
   - 绝对路径；
   - Windows 盘符与 UNC；
   - 重名文件；
   - 超长文件名；
   - multipart crafted filename。

---

# 4. P1：强烈建议优化

## 4.1 补充主聊天链路的无浏览器 integration smoke

**状态：已完成（第二阶段）**

实现：`lib/chat-prompt-lifecycle.ts`（纯状态机 + throttler harness）与 `scripts/smoke-chat-lifecycle.ts`，并入 `npm run test:agent-stream`。覆盖 happy path、tool 文本屏障、retry 保持忙碌、最终失败/`prompt_error`、abort·destroy·缓冲重放、slash `prompt_settled`，以及 `useAgentSession`/`rpc-manager` 源码契约检查。

浏览器 E2E 能覆盖真实点击、对话框和页面渲染，但会引入浏览器运行时、额外依赖、服务启动等待和更长的 CI 时间。当前项目明确不采用需要安装 Chromium/Playwright 浏览器的测试方案：交互和对话框问题由实际使用反馈及发布前人工检查发现，不把低频 UI 问题转化为每次开发都要承担的测试成本。

近期只补低成本、无浏览器的聊天集成 smoke：

```text
mock AgentSession
  → 调用聊天 API/事件适配层
  → 产生 agent_start / message_update / tool 事件
  → 产生 agent_end / agent_settled
  → 验证事件顺序、状态收口和错误路径
```

建议覆盖：

1. `agent_start → message_update → agent_end → agent_settled` 的正常顺序；
2. tool start/end 不越过待刷新文本；
3. retry 期间 `agent_end` 不提前结束 prompt；
4. `prompt_error` 和最终 retry failure 正确收口；
5. abort、wrapper teardown 和事件缓冲不会留下运行中状态；
6. mock 全部在进程内完成，不访问真实模型、不启动浏览器。

这类测试不能证明对话框几何、焦点和视觉渲染正常，但能守住更容易造成会话卡死或状态错误的协议边界，同时把新增测试耗时控制在秒级。UI 仍按 `docs/operations/ui-visual-validation.md` 做发布前或相关改动后的人工抽查。

---

## 4.2 聊天/provider 错误体验产品化

**状态：已完成（第三阶段）**

实现：`lib/chat-provider-errors.ts` 将鉴权/配额/网络/模型不存在/空响应映射到 `ERROR_CODES.chat*`；failure card 显示分类标题、本地化文案、建议动作与可折叠技术详情，并提供「去模型设置」。

当前项目并非完全没有错误码体系：`lib/i18n/error-codes.ts`、Browser、Automation、MCP、WebTools 等领域已经存在稳定错误码和本地化能力。

缺口主要位于聊天失败链路：

- `hooks/useAgentSession.ts` 多处将异常转成原始字符串；
- `ChatWindow.tsx` 的 failure card 除 empty-completed 外主要展示 provider/SDK 原文；
- 用户难以区分鉴权、配额、网络、模型不存在和服务端异常。

### 建议

扩展现有错误体系，不新建平行框架。第一批只支持高频五类：

| 类别 | 用户标题 | 建议动作 |
| --- | --- | --- |
| 鉴权失败 | 模型认证失败 | 打开 Models、重新登录或更新 API Key |
| 配额/限流 | 当前账号额度不足或请求过快 | 查看额度、切换账号/模型、稍后重试 |
| 网络失败 | 无法连接模型服务 | 检查代理和网络配置 |
| 模型不存在 | 当前模型不可用 | 重新发现模型或切换模型 |
| 空响应/协议异常 | 模型未返回有效内容 | 自动重试、显示技术详情 |

技术原文保留在可折叠详情中，便于排障。

---

## 4.3 首启与模型可用性引导

**状态：已完成（第三阶段）**

实现：`lib/chat-send-readiness.ts` + Composer 格挡条；无工作区/模型加载中/无可用模型/未选模型时禁用普通发送，并引导打开 Models。空模型列表覆盖“未配置 Key/未登录导致无可用模型”的主路径；发送后的鉴权失败仍走 4.2 分类卡片。

当前空态主要完成“选择目录 → 新建会话”，新会话编辑器没有在无可用模型时形成完整闭环。发送动作仍可能进入后端后才暴露配置错误。

建议增加轻量检查，而不是大型新手教程：

1. 已选择工作目录；
2. 至少存在一个模型；
3. 存在默认模型或已显式选择模型；
4. 对需要认证的 provider 给出“未登录/未配置 Key”状态；
5. 条件不满足时禁用普通发送，并提供“去 Models 修复”。

目标是让首次用户完成：

```text
选项目 → 模型可用 → 发第一条消息 → 收到回复
```

---

## 4.4 Usage 与文件根全量扫描护栏

**状态：已完成（第四阶段）**

实现：
- `lib/allowed-roots.ts` 优先 `getSessionIndexCwdRoots()`（active index cwd 集合），失败时回退 `listAllSessions()`；5s 缓存与 `registerAllowedRoot` / `~/pi-cwd-*` 语义不变。
- `lib/usage-stats.ts` 候选会话优先 session index（支持 cwd 过滤），JSONL usage 改为 SDK-free 行扫描；结果增加 `scanSource` / `durationMs`，Usage 弹窗展示索引加速与耗时。
- 可重复基线：`npm run test:scale-baseline`（默认 40 sessions；可用 `SCALE_SESSION_COUNT` / `SCALE_MESSAGE_COUNT` / `SCALE_LARGE_MESSAGE_COUNT` 放大到 1k/5k/10k）。

---

## 4.5 同会话多 Tab 协调

**状态：已完成（第四阶段）**

实现：
- `lib/session-tab-coordination.ts` + `hooks/useSessionTabLock.ts`：BroadcastChannel 心跳、过期 prune、字典序选举 writer、显式 `claim_write` 接管；
- `ChatWindow` 只读横幅 +「接管编辑」；`ChatInput` `writeLocked` 禁用发送/steer/follow-up；fork/navigate/continue 同步只读；
- 纯逻辑回归：`npm run test:session-tabs`；
- 服务端/SDK 并发仍是硬兜底（未改 RPC 语义）。

---

## 4.6 单实例部署防误配

**状态：已完成（第五阶段）**

实现：
- `bin/runtime-options.js`：`detectMultiInstanceRisk` 识别 `NODE_APP_INSTANCE` / `NODE_UNIQUE_ID` / `WEB_CONCURRENCY>1` / `instances>1`；默认拒绝，`PI_WEB_ALLOW_MULTI_INSTANCE=1` 紧急覆盖仍打强警告；
- `bin/pi-web.js` + `instrumentation.ts`：启动前/注册时强制单实例；Ready/boot 打印 `pid` / `instanceId` / `mode` / `bind`；
- `lib/process-runtime.ts` + `GET /api/health`：公开最小 health（pid、instanceId、liveSessions、sseListeners、scheduler role；无 session id/路径/密钥）；
- 文档：`docs/deployment/README.md`、`docs/operations/troubleshooting.md` 明确 sticky routing ≠ 多副本受支持；
- 回归：`npm run test:server-auth`、`npm run test:runtime`。

需要区分两个事实：

- 普通聊天会话的 wrapper、start lock、SSE listener 是进程内状态，多实例负载均衡不受支持；
- Automation 已经具有跨进程 scheduler lock、leader/standby、claim、lease 和 fencing，不应描述成“仅进程内 leader”。

---

## 4.7 收口 UI Iteration 8 浏览器矩阵

**状态：已确认**

`docs/plans/README.md` 中视觉系统和 UI quality 计划仍标记为 active，剩余项主要是固定 viewport、代表主题、焦点、Portal 和组合层级的真实浏览器检查。

建议严格遵循 `docs/operations/ui-visual-validation.md`：

- 只修阻塞交互、不可达内容、焦点错误、严重对比度和层级问题；
- 不在收口阶段扩展成整体视觉重做；
- 记录浏览器、主题、viewport、失败项和截图位置；
- 完成后关闭对应 active plan，避免长期悬空。

---

## 4.8 长会话性能基线

**状态：已建立服务端/解析基线（第四阶段）；浏览器渲染优化仍待指标驱动**

已完成：
- `npm run test:scale-baseline` 含长 JSONL 行解析计时（默认 largeMessages=500，可用 env 放大）；
- Usage 路径不再经 `SessionManager.open` 全量打开 transcript。

仍不做：未引入虚拟列表。浏览器侧 100/500/1000 可见消息、大 Markdown/Mermaid 的 DOM 指标需在真实浏览器中另采；只有证明 DOM 是主瓶颈后再选分段加载 / 折叠 tool / 延迟高亮 / 虚拟化。

---

# 5. P2：按维护节奏处理

| 条目 | 当前判断 | 建议 |
| --- | --- | --- |
| CLI-only 命令 | 已确认只有 badge/样式，仍可插入并发送 | 选择时阻止发送，明确提示到 CLI 使用 |
| Session Changes 范围 | 已确认只跟踪内置 edit/write | UI 标注范围；不要伪装成完整 Git 变更 |
| idle SSE 重连 | 已确认 `onerror` 只在 `agentRunning` 时主动重连 | 增加连接状态和指数退避；先确认 idle 事件真实需求 |
| Browser 配对体验 | 产品建议 | 非 loopback 时说明原因；本机提供扩展安装→配对→绑定引导 |
| Settings/Models 信息架构 | 产品建议 | 常用项置顶、高级项折叠、增加搜索；不重写业务逻辑 |
| 大文件渐进拆分 | 已确认 | 只在相关功能改动时按域抽取，避免纯重构大 PR |
| smoke 统一报告 | 已确认 | 增加统一 runner/CI 汇总；是否输出 JUnit 取决于 CI 需求 |
| Markdown 外链 | 原描述不准确 | 当前普通外链不新开窗口；若未来使用 `_blank`，同时加 `noopener noreferrer` |
| 成功/错误反馈规范 | 产品建议 | 统一 toast、inline notice、dialog 的使用边界 |
| soak/压力测试 | 待量化 | 发布前或长期服务器场景再增加，不作为当前所有 PR 门禁 |

---

# 6. 代码质量观察

## 6.1 大型中枢文件

抽查时以下文件约为 1.5k–3.8k 行：

- `components/ModelsConfig.tsx`
- `hooks/useAgentSession.ts`
- `components/AppShell.tsx`
- `lib/automation-service.ts`
- `lib/automation-runner.ts`
- `lib/automation-store.ts`
- `lib/workflow-store.ts`

这说明改动冲突和回归面可能逐步增加，但“行数大”不是单独重构理由。

建议策略：

- Models：按 provider editor、账号、pricing、发现/测试流程拆视图；
- `useAgentSession`：优先抽 SSE 生命周期、extension UI 状态、错误映射；
- AppShell：优先抽顶栏面板状态和 Inspector/Terminal 组合状态；
- Automation/SnFlow：保持领域隔离，只抽稳定且真正重复的 path/lock/revision 原语。

## 6.2 不建议做

- 重写 session/RPC 主链路；
- 合并 Automation 与 SnFlow；
- 为了统一而引入大型状态管理或 UI 框架；
- 没有指标就进行全局虚拟化；
- 仅为降低文件行数制造大量薄包装模块。

---

# 7. 安全模型：应保持的资产

以下设计已经较成熟，应避免在优化中被削弱：

- 默认绑定 `127.0.0.1`；
- 非回环/server 模式启用 Access Key；
- Server 模式默认要求有效 HTTPS；
- 状态修改请求执行 Exact Origin 校验；
- `server-access.json` 不保存明文 key；
- Automation、native picker、browser bridge 不因全局登录而放宽 loopback 门；
- Automation 工具为审批快照与 live policy 的交集；
- 文件读取使用 allowed roots；
- Markdown 使用 `rehype-sanitize`；
- Browser 工具不暴露任意 JS、Cookie、响应 body 或原始认证头。

注意：上传文件使用独立的 `~/.pi/agent/uploads` 存储边界，并不等同于普通项目 allowed roots。正确目标应是“始终留在受控上传根内”，而不是强制上传到项目 cwd。

---

# 8. 推荐实施顺序

## 第一阶段：立即安全修复

1. 修复上传文件名路径逃逸；✅（`lib/file-upload.ts` + `app/api/files/upload/route.ts`：basename 净化、包含校验、独占 `wx` 写入、展示名/存储名分离）
2. 增加 multipart 和平台路径回归测试；✅（`npm run test:file-upload` → `scripts/smoke-file-upload.ts`）
3. 复查上传根、清理逻辑和本地跨 Origin 行为。✅（上传根改为 `PI_CODING_AGENT_DIR/uploads`；cleanup 仅遍历 root 下一层 session 目录并做包含校验；跨 Origin 仍由 Server 模式 Proxy Exact Origin 门处理，本地 loopback 模式保持既有策略）

## 第二阶段：守住核心价值

4. 增加无浏览器的主聊天 integration smoke；✅（`lib/chat-prompt-lifecycle.ts` + `scripts/smoke-chat-lifecycle.ts`）
5. 覆盖 `agent_settled`、tool 和 failure/retry；✅（happy path / tool 屏障 / retry 保持忙碌 / 最终失败 / abort·destroy·缓冲重放 / slash `prompt_settled`）
6. 纳入 CI，保持纯 mock、秒级执行且不增加浏览器依赖。✅（并入 `npm run test:agent-stream`）

## 第三阶段：降低首次失败成本

7. 扩展现有 error-code 体系到聊天/provider；✅（`lib/chat-provider-errors.ts` + `ERROR_CODES.chat*` + failure card 分类标题/动作）
8. 增加无模型/无认证时的发送前检查；✅（`lib/chat-send-readiness.ts`：无工作区/加载中/无模型/未选模型时禁用发送；空模型列表覆盖未配置认证）
9. 提供 Models 修复入口。✅（Composer 格挡条 + failure card「去模型设置」→ AppShell 打开 ModelsConfig）

## 第四阶段：处理规模增长

10. 建立 Usage、allowed-roots、长 JSONL 和长会话基线；✅（`npm run test:scale-baseline`，env 可放大到 1k+）
11. 根据数据优化扫描和渲染；✅（allowed-roots / Usage 复用 session index；Usage JSONL SDK-free 扫描；浏览器虚拟列表仍按指标驱动，未做）
12. 增加多 Tab 提示及写权限策略。✅（BroadcastChannel + 只读横幅 + 接管编辑；`test:session-tabs`）

## 第五阶段：收口运维与视觉债务

13. 增加单实例强提示和最小 health 信息；✅（cluster 拒绝启动 + Ready 身份日志 + `GET /api/health`）
14. 完成 UI Iteration 8 浏览器矩阵；
15. 再按实际维护热点渐进拆分大型文件。

---

# 9. 未来 30 天建议清单

| 顺序 | 事项 | 优先级 | 验收结果 |
| --- | --- | --- | --- |
| 1 | 上传路径边界修复 | P0 | ✅ crafted filename 无法写出 upload session 目录 |
| 2 | 上传安全回归集 | P0 | ✅ Windows/POSIX 路径、绝对路径、重名测试进入 `test:file-upload` |
| 3 | 主聊天 integration smoke | P1 | ✅ mock 会话从启动到 `agent_settled` 的事件和状态正确收口（`test:agent-stream`） |
| 4 | retry/failure smoke | P1 | ✅ 重试成功和最终失败的生命周期均有自动化守护 |
| 5 | 聊天错误分类 | P1 | ✅ 鉴权/配额/网络/模型/空响应有稳定错误码和动作（`test:chat-errors`） |
| 6 | 首启模型格挡 | P1 | ✅ 无可用模型时不能进入无提示失败 |
| 7 | Usage/扫描性能基线 | P1 | ✅ `test:scale-baseline`（默认小集 + env 放大）；index 加速 allowed-roots/Usage |
| 8 | 多 Tab 会话提示 | P1 | ✅ 双开只读提示 + 接管编辑（`test:session-tabs`） |
| 9 | 单实例/health 运维护栏 | P1 | ✅ cluster 拒绝；Ready 日志；`/api/health` 含 pid/instanceId/liveSessions/sse/scheduler |
| 10 | UI Iteration 8 收口 | P1 | 代表主题和固定 viewport 矩阵有完成记录 |

---

# 10. 总结

前四阶段与第五阶段第 13 项（单实例/health 运维护栏）已落地。下一阶段优先：

1. **UI Iteration 8 浏览器矩阵收口；**
2. **按真实维护热点渐进拆分大型中枢文件；长会话 DOM 优化仍需浏览器侧指标后再动。**

浏览器 E2E 不纳入近期计划，对话框、布局和真实交互问题继续通过实际使用反馈与人工视觉检查处理。

---

## 参考依据

- `README.md`
- `AGENTS.md`
- `docs/architecture/overview.md`
- `docs/modules/frontend.md`
- `docs/modules/api.md`
- `docs/modules/library.md`
- `docs/deployment/README.md`
- `docs/operations/troubleshooting.md`
- `docs/operations/ui-visual-validation.md`
- `docs/plans/README.md`
- `package.json`
- `proxy.ts`
- `bin/pi-web.js`
- `bin/runtime-options.js`
- `app/api/files/upload/route.ts`
- `hooks/useAgentSession.ts`
- `components/ChatWindow.tsx`
- `components/MarkdownBody.tsx`
- `lib/rpc-manager.ts`
- `lib/automation-scheduler.ts`
- `lib/usage-stats.ts`
- `lib/file-access.ts`
- `lib/i18n/error-codes.ts`
