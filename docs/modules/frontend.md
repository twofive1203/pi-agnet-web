# Frontend Module Map

## Components

| File | Purpose |
| --- | --- |
| `components/I18nProvider.tsx` | Client locale provider (`zh` / `en`), `t()` translator, and `useI18n` / `useT` hooks. Preference is stored in `localStorage` key `pi-locale`; first visit follows the browser language. |
| `components/AppDialogProvider.tsx` | Unified application dialog provider wrapping the app root. Renders a body portal for `alert`, `confirm`, and `prompt` dialogs with serial queue, Escape/Enter keyboard handling, overlay click cancel, danger-tone confirm styling, auto-focused prompt input, and i18n-aware title/button labels. Exposes `useAppDialog()` hook returning `{ alert, confirm, prompt }` with Promise-based API. |
| `components/AppShell.tsx` | Top-level layout and sole source of truth for `activeCwd`. Concept-B workbench shell: three floating card panels (left context / center chat / right inspector) built on `--bg-soft/--bg-card/--line/--text-2/--accent-soft` aliases in `app/globals.css`. The old full-width top bar is merged into the center panel's `top-context` strip (breadcrumb, branch/export/system/usage chips, theme, locale, Automation badge, terminal); an `observe-bar` of chips (Changes / Git / Subagents / Todo) opens the corresponding right Inspector tab. Right panel is a multi-tab Inspector — Changes (`InspectorChangesPanel`), Preview (file tabs + `FileViewer`), Git (`GitPanel`), SnFlow (`WorkflowPanel`), Agents (`StoredSubagentPanel`) — ending the old files-vs-SnFlow hard exclusivity; Alt+click on the SnFlow tab still creates a task from chat. Owns URL state, tab management, language toggle, Web Terminal bottom-dock toggling, SnFlow-task-to-chat context insertion, body-portaled top-bar auxiliary panel (system prompt) that stays visible outside the mobile horizontal scroller, and a shared right-panel width persisted under `localStorage` key `pi-web-right-panel-width-v1`. Wide desktop (≥920px) docks the panel inline with pointer/keyboard resize and viewport clamping; narrow desktop (641–919px) uses an overlay drawer so chat is not compressed and hides the resize handle; ≤640px keeps the existing full-width mobile drawer. Workspace picker changes go through `onActiveCwdChange` (may clear cross-cwd session/new-session state); session select/create/restore/fork/delete paths sync `activeCwd` without the picker wipe branch so URL restore does not double-bump `sessionKey`. Session deletion reads latest `selectedSession`/`activeCwd` via refs so WorkTree bulk-delete fallback and newer project selection are never overwritten by a stale callback snapshot. |
| `components/InspectorChangesPanel.tsx` | Embedded “Changes” view for the right Inspector tab. Fetches `/api/sessions/{id}/changes`, polls while the agent runs, refreshes on `refreshKey` bumps, and opens per-file diffs via `FileDiffModal`. Uses the same data as the floating `SessionChangesFloatingPanel` pill in the chat. |
| `components/ThemePicker.tsx` | Compact body-portaled appearance picker opened from the top bar. Applies system, light, dark, curated professional skins, or opt-in MetaCubeXD-inspired Twilight, Night, Daisy Dark, and Dracula skins immediately through `useTheme`; preference stays browser-local. |
| `components/SessionSidebar.tsx` | Controlled session sidebar shell (workspace cwd via `activeCwd` / `onActiveCwdChange` from AppShell). Owns business actions, top-level UI state, data-hook wiring, and composition of `components/sidebar/*`. Adds concept-b nav pills (Sessions / Files / Archive) and a client-side session search box; `filesPill`/`archivePill` optional props open the explorer pane / archived section from AppShell. Session browsing data comes from `hooks/useSessionBrowser`. Fork tree rendering uses `lib/sidebar-session-tree.ts`. |
| `components/sidebar/sidebar-utils.ts` | Pure cwd picker helpers/types: ordered cwds, shorten path, picker row build/group/filter, explorer height constants/storage. |
| `components/sidebar/WorkspacePicker.tsx` | Memoized workspace header, action row (new session / WorkTree / refresh / archive-all menu), cwd dropdown/search/custom path, and WorkTree context menu. Picker UI state is local; cwd/worktree actions are raised via props. |
| `components/sidebar/WorktreeBadge.tsx` | Memoized WorkTree badge chip shared by picker rows and session items. |
| `components/sidebar/SessionList.tsx` | Memoized active session tree/list with rename, multi-select archive bar, load-older pagination, and memoized leaf `SessionItem` / `SessionTreeItem` rows (rename focus + context menu preserved). |
| `components/sidebar/ArchivedSessionSection.tsx` | Memoized archived section expand/load-more and memoized muted archived leaf rows with unarchive/delete. |
| `components/sidebar/SidebarExplorerPane.tsx` | Memoized Explorer pane + desktop resize handle; height persisted under `localStorage` key `pi-web-explorer-height-v1`. At ≤640px the resize handle stays hidden and expanded Explorer shares remaining height with the session list. |
| `components/ChatWindow.tsx` | Memoized message list, SSE streaming, fork/navigate logic. Historical message rows and derived tool-result/stat projections retain stable identities during streaming, so token updates rerender only the live row instead of reparsing the full Markdown history. Parent-shell subagent progress updates do not rerender the chat when its session props and callbacks are unchanged. Shows archived banner and disables input for archived sessions. Hosts Pi extension status chips, generic widget stacks around the composer, the floating todo panel, and body-portaled dialog/toast hosts for extension UI requests. Reports agent running state and extension todo-widget presence up to AppShell via `onAgentRunningChange`/`onTodoActiveChange` for the observe-bar indicators. |
| `components/ChatInput.tsx` | Input bar, model dropdown, thinking level, tool preset, image upload, file-reference chips, SnFlow task resume prompt injection, and bounded, scrollable slash-command autocomplete for extension commands, prompts, and skills. Extension commands may show CLI-only / Partial badges from server `webSupport` metadata. |
| `components/ExtensionStatusBar.tsx` | Compact chip row for Pi extension `setStatus` keys near the chat composer. |
| `components/ExtensionWidgetStack.tsx` | Multi-line text cards for Pi extension `setWidget` content at `aboveEditor` / `belowEditor` placements; reserves the standard `todo-list` widget for the floating task panel. Chat hides powerbar chrome and suppressed pi-subagents HUDs (`subagent-fleet-status`, `subagent-async`); those are also dropped in `lib/extension-web-ui.ts`. |
| `components/ExtensionTodoPanel.tsx` | Floating session todo panel for the standard `todo-list` widget: draggable compact capsule entry, expandable desktop panel, mobile bottom sheet, persisted/clamped position, progress bar, and parsed task rows. |
| `components/ExtensionDialogHost.tsx` | Body-portaled modal for blocking extension dialogs (`confirm`, `select`, `input`, `editor`) with keyboard navigation and Escape cancel; replaces `window.confirm` / `window.prompt`. |
| `components/ExtensionToastHost.tsx` | Body-portaled toast stack for non-blocking extension `notify` events (info/warning/error); replaces `window.alert`. |
| `components/ChatGptUsagePanel.tsx` | Optional semi-transparent top-bar ChatGPT/Codex quota panel; renders its viewport-bounded details popover through `document.body` so it is not clipped by the mobile top-bar scroller, reads and periodically revalidates cached active-account usage and reset-credit availability, reloads accounts on expand, lists saved accounts with quick activation, supports manual quota refresh and confirmed reset-credit consumption for the active account, and shows backend auto-refresh scheduler/lock maintenance state. |
| `components/GrokUsagePanel.tsx` | Optional top-bar Grok subscription usage panel (gated by `pi-web.json.grok.usagePanelEnabled`). Aligns with ChatGPT panel interaction pattern: popover via body portal, manual refresh only, cache-first load. Shows monthly used/limit/remaining/utilization/reset time, optional weekly credits, and error/not-configured states. Billing accepts OAuth from either the built-in xAI provider or the `grok-cli` extension. Controlled by Settings → Grok toggle. |
| `components/ChatGptWarmupDialog.tsx` | ChatGPT/Codex warmup management dialog opened from the saved-account management area; supports manual multi-select warmup, scheduled warmup account/time settings, and recent manual/scheduled run history. |
| `components/MessageView.tsx` | Memoized renderer for user, assistant, tool-call, and tool-result messages; stable historical props skip rerenders while the live assistant message streams. |
| `components/BranchNavigator.tsx` | In-session branch switcher; its inline top-bar panel uses a fixed body portal so mobile toolbar overflow does not clip the branch tree. |
| `components/ChatMinimap.tsx` | Scroll minimap beside message list. |
| `components/ToolPanel.tsx` | Tool presets and preset inference helpers. |
| `components/ModelsConfig.tsx` | Modal for editing `models.json`, discovering OpenAI-compatible custom-provider models from `/models` into the staged provider model list, provider/model custom HTTP headers (key/value editor written to `headers`), default `User-Agent` seeding for `openai-responses` (`codex-tui/0.125.0 (Windows 10.0.26100; x86_64) WindowsTerminal (codex-tui; 0.125.0)`) and `anthropic-messages` (`claude-cli/2.1.220 (external, cli)`) when unset, OAuth/API-key auth, structured Grok CLI subscription usage via the shared server-owned billing API (cache-first + manual refresh), ChatGPT Plus/Pro saved-account add/import, activation, temporary account selection for the subscription/usage panel, remarks, extra-info dialog, cached quota reset display with inline mini usage pies, manual quota refresh, inactive-account deletion, raw/CPA/SUB2API account JSON import via shared converters, manual pi.dev pricing sync via `POST /api/model-pricing` with a compact status row and sync/catalog icon buttons, automatic safe price/context-window filling, and manual candidate matching for ambiguous custom-provider model ids. |
| `components/GitPanel.tsx` | Git status dropdown panel showing branch, previewable/selectable commit graph by selected local branch, selected-commit metadata and changed files, staged/unstaged changes, untracked files, stash, and local branch switching with an explicit Switch button. |
| `components/CommitGraph.tsx` | Git commit graph renderer with lane visualization, refs, hover tooltips, and optional selected-commit callbacks for the Git panel. |
| `components/GitCommitDiffModal.tsx` | Git commit file-diff adapter that fetches one selected commit file diff, formats commit/file metadata and fallback labels, and renders the shared diff modal. |
| `components/SkillsConfig.tsx` | Modal for browsing/installing skills. |
| `components/ExtensionsConfig.tsx` | Modal with Resources (packages/extensions/tools/commands/skills/prompts/diagnostics from `/api/pi/resources`) and Settings (registered extension settings + `settings-extensions.json` edit via `/api/pi/extension-settings`). Opened from the sidebar Extensions button. |
| `components/SettingsConfig.tsx` | Settings modal for interface language (`zh`/`en`), WorkTree defaults, Usage scan scope, Web Terminal enablement/shell/env settings including Unix and Windows shell choices plus raw/AI env parsing model controls, ChatGPT usage panel and backend auto-refresh settings, Grok usage panel toggle, Editor implementation/shortcut settings, native Pi subagent model settings (`AgentsConfig`), MCP adapter-native configuration (`McpConfig`), and SnFlow preferences (include-archived default) plus project inspection/init/update (versioned extension/skill/agent assets; no global enable switch). Legacy `pi-web.json.trellis` data is left untouched by settings saves. ChatGPT warmup schedule is managed from `ChatGptWarmupDialog` and preserved by settings saves. |
| `components/AgentsConfig.tsx` | Native pi-subagents model configuration section in the Settings modal. Owns its own load/save/dirty/error state, supports user-global and project scopes, loads/saves the `subagents` section of Pi `settings.json`, displays discovered agents with source badges, and provides per-agent model/thinking/fallback-model controls with inherit/clear semantics. |
| `components/McpConfig.tsx` | Independent MCP settings panel in the Settings modal. Owns its own load/save/dirty/error/conflict state (not part of the `pi-web.json` save payload). Edits adapter-native targets (user/project shared + Pi overrides) through `/api/mcp/config`, shows precedence diagnostics, server CRUD for stdio/HTTP/socket transports, masked secret preserve/replace/clear controls, package configured vs runtime-unknown messaging, and `/reload` activation guidance. |
| `components/SubagentPanel.tsx` | Top-bar subagent activity panel. Summary rows show bounded progress/routing only. Expansion performs abortable, fingerprint-aware, 32-entry-cached detail requests; active expanded details refresh every 2 seconds, recursive direct-child loading is limited to depth 3, and server truncation is surfaced. Closing the panel aborts all requests and releases its full-run subscription/cache. |
| `components/SubagentObservation.tsx` | Isolated external-store consumers: badge subscribes only to cached running/completed/failed counts; the full panel subscribes to run summaries only while the panel is mounted/open. |
| `components/WorkflowPanel.tsx` | WebUI-owned SnFlow drawer: per-project initialize empty state, optional update-available hint, create/select tasks under `.pi/snflows/tasks/` (including one-click create-from-current-chat), edit requirements/design/plan Markdown, open a saved task document in the standalone file page, mark ready, request an exact selected-task-bound implement/check instruction, and send it to the current chat for a visible native `subagent` tool call. It renders one-level parent/child task trees, supports creating a child from the selected task, shows child completion counts, and preserves parent context when filtering. It polls persisted run status, records commit metadata, completes, and archives tasks. |
| `components/WorkflowSessionWidget.tsx` | Floating session-scoped SnFlow task capsule (plan/execute/finish), shown only when the selected non-archived chat has an exact pointer binding or explicit transcript evidence for the task; opens the SnFlow drawer focused on that task. |
| `components/SessionChangesFloatingPanel.tsx` | Floating chat-session file-change panel that lists tracked edit/write file changes and opens per-file diffs. |
| `components/FileDiffModal.tsx` | Session changed-file diff adapter that fetches one tracked file diff, formats session metadata and fallback labels, and renders the shared diff modal. |
| `components/DiffModal.tsx` | Shared read-only diff modal shell with close-on-Escape, overlay click close, source-specific header slot, loading/error/fallback handling, and Unified/Side-by-side mode controls defaulting to side-by-side. |
| `components/DiffView.tsx` | Shared diff renderer switch that renders unified diffs through `UnifiedDiffView` or parsed side-by-side diffs through `SideBySideDiffView`. |
| `components/SideBySideDiffView.tsx` | Theme-aware side-by-side unified diff parser/renderer with old/new line numbers, aligned modification rows, and metadata/hunk rows. |
| `components/UnifiedDiffView.tsx` | Theme-aware unified diff renderer for added, removed, hunk, header, and context lines. |
| `components/TerminalPanel.tsx` | Bottom-dock Web Terminal workspace using xterm; manages ephemeral multi-tab terminal sessions, per-pane tab strips, tab renaming, nested drag-to-split panes, pane and dock resizing, minimize/restore, app-local fullscreen, and destructive close confirmation while reusing existing terminal session APIs per tab. It is ordinary Web Terminal UI, not an extension-specific tool bridge. |
| `components/BrowserBindingTrigger.tsx` | Composer-row Browser button next to image/file attachments. Owns `useBrowserBridgeStatus` polling (15s idle / 4s while open), shows a red/green/orange status dot, and opens a body-portaled binding popover. |
| `components/BrowserBindingPanel.tsx` | Session-level browser control UI inside the composer popover: enable/pair extension, create pending tab bind requests, list primary/additional bindings, toggle read-only debug, and revoke. When opened from the trigger it reuses shared status props (no second poller); standalone use enables its own poller only while expanded. Uses `/api/browser/*` only (not chat SSE). |
| `components/ModelPricingCatalog.tsx` | Searchable, provider-filterable read-only view of the locally cached pi.dev model pricing catalog, including context-window and token-price columns. |
| `components/UsageStatsModal.tsx` | Token/cost usage statistics modal with active/archive scan counts, parent-attributed native subagent costs, main/subagent splits, and rounded M-token conversions. |
| `components/FileExplorer.tsx` | On-demand file tree inside the sidebar; root loads are abortable and workspace changes clear old roots before applying the new response. A row click keeps the existing right-panel preview, while the hover action opens that file in the standalone page. |
| `components/FileViewer.tsx` | Shared file content viewer/editor used by right-panel tabs and the standalone page; routes media/document previews, orchestrates text editing state, accepts an initial source line, and exposes Java implementation lookup results. |
| `components/StandaloneFileViewer.tsx` | Full-viewport shell for `/file?path=...`; provides workspace navigation/path copy chrome and reuses `FileViewer`. Root-level historical Windows links such as `/D:/repo/File.java:11` redirect here through `app/[...legacyFilePath]/page.tsx`. |
| `components/MonacoFileEditor.tsx` | Monaco-backed source editor for text files, including language mapping, basic completions, word wrap, theme, and line-selection callbacks. |
| `components/FileIcons.tsx` | Monochrome SVG icons for files/folders. |
| `components/MarkdownBody.tsx` | Markdown, KaTeX, Mermaid, and syntax highlighting renderer. Recognized loopback/absolute Windows file links are normalized through `lib/file-viewer-url.ts` and opened in the standalone file page; normal web links are unchanged. |
| `components/TabBar.tsx` | Chat and open-file tab bar. |

## Hooks

| File | Purpose |
| --- | --- |
| `hooks/useSessionBrowser.ts` | Sidebar session browser data layer: project summaries, mtime-cursor paged active sessions (`loadMore`), paged archived sessions, abort/cwd isolation, and outside-window selected-session patches via `GET /api/sessions/[id]`. |
| `hooks/useAgentSession.ts` | Central chat/session hook: data loading, SSE, streaming state, commands, tools, model metadata, thinking levels, and bounded subagent summaries. Hidden tabs coalesce cumulative `message_update` snapshots to 500 ms while flushing before lifecycle events/when visible; completed-message usage stats are memoized by message-list identity. Ordinary server progress no longer carries output; compatibility/final/persisted output is capped to an 8k preview while full bounded detail loads on expansion. Summary state stays outside React chat state and flushes at 300 ms, with terminal/attention updates immediate. The AppShell callback writes an external store rather than top-level React state. Optional browser metrics use `NEXT_PUBLIC_PI_WEB_SUBAGENT_OBSERVABILITY=1`. The hook also owns extension UI state and response bridging. |
| `hooks/useTheme.ts` | Browser-local appearance store for system/light/dark and curated skins, including MetaCubeXD-inspired Twilight, Night, Daisy Dark, and Dracula skins, with OS color-scheme updates, reduced-motion-aware view-transition animation, and a resolved `isDark` compatibility projection for Monaco/Markdown consumers. |
| `hooks/useDragDrop.ts` | Drag-and-drop image attachment handler. |
| `hooks/useAudio.ts` | Sound toggle and completion chime playback. |
| `hooks/useAutoScroll.ts` | Persisted chat auto-stick-to-bottom preference used by the message list and input toggle. |
| `hooks/useBrowserBridgeStatus.ts` | Shared browser-bridge status poller for the composer Browser control. Dedupes in-flight fetches, slows idle polling to 15s, and speeds up to 4s while the popover is active so Trigger + Panel do not double-hit `/api/browser/status`. |

## Styles

Global CSS lives in `app/globals.css`. Components may reference these CSS variables directly:

```text
--bg --bg-panel --bg-hover --bg-selected --bg-subtle --border
--text --text-muted --text-dim --accent --accent-hover
--user-bg --assistant-bg --tool-bg --font-mono
```

They are also mapped to Tailwind `--color-*` utility aliases. The appearance system stores the selected preference under `localStorage["pi-theme"]`. It toggles `dark` on `document.documentElement` for binary editor/renderer compatibility and sets `data-theme-skin` for curated semantic-token overrides. The opt-in MetaCubeXD family (`twilight`, `night`, `daisy-dark`, and `dracula`) additionally scopes layered shell surfaces, accent-tinted active states, and tactile motion to its own skin selectors, so other skins retain their existing chrome. Night, Daisy Dark, and Dracula preserve the DaisyUI 5.7.0 base/content/primary color relationships without adding DaisyUI as a dependency. The pre-hydration script in `app/layout.tsx` derives its skin list from the registered theme preferences and applies the selected skin before first paint.

Desktop split sizing uses shared `.panel-resize-handle` styles plus `--right-panel-width` on `.app-shell-root`. Inline resizable right panel applies at `min-width: 920px`. Between `641px` and `919px` the right panel is a fixed overlay drawer (no vertical resize handle) so the chat column is not squeezed below its minimum. At `max-width: 640px`, all resize handles are hidden and mobile drawer/full-width rules continue to override dynamic panel sizes; Explorer still equal-shares remaining sidebar height when expanded.

## Internationalization (i18n)

- **Supported locales**: `zh` (default when browser is Chinese), `en`.
- **Provider**: `components/I18nProvider.tsx` wraps the app in `app/page.tsx`.
- **Catalogs**: `lib/i18n/messages/*` nested dictionaries; lookup via dotted keys such as `sidebar.archiveAllSessions`.
- **API**: `const { locale, setLocale, t } = useI18n()`; `t("key", { name })` interpolates `{name}` placeholders.
- **Persistence**: `localStorage["pi-locale"]`; boot script in `app/layout.tsx` sets `document.documentElement.lang` early to reduce flash.
- **Switcher**: top-bar `EN`/`中` button in `AppShell`, and Settings → Language section.
- **Coverage status**: shell/chat/sidebar/git/terminal/diff and shared chrome strings are wired. Large settings/models panels still contain mixed hard-coded Chinese/English copy and should continue migrating onto `settings.*` / `panels.*` keys.

## Automation UI

| File | Purpose |
| --- | --- |
| `components/AutomationPanel.tsx` | Global Automation drawer: task list/detail/editor, runs, inbox. |
| `components/AutomationTaskEditor.tsx` | Explicit Save draft / Review & activate editor. |
| `components/AutomationToolPicker.tsx` | Dimensional risk tool picker. |
| `components/AutomationRunList.tsx` | Run history list. |
| `components/AutomationRunViewer.tsx` | Read-only transcript/promote viewer (no RPC wrapper). |
| `components/AutomationInboxBadge.tsx` | Unread badge helper. |
| `hooks/useAutomations.ts` | Control session, polling, approval, lifecycle actions. |
| `lib/i18n/messages/automation.ts` | zh/en Automation copy. |
