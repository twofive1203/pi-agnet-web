# Frontend Module Map

## Components

| File | Purpose |
| --- | --- |
| `components/I18nProvider.tsx` | Client locale provider (`zh` / `en`), `t()` translator, and `useI18n` / `useT` hooks. Preference is stored in `localStorage` key `pi-locale`; first visit follows the browser language. |
| `components/ServerUnlockForm.tsx` | Minimal bilingual unlock form for server-mode access keys. No AppShell/session fetches; blocks entry when HTTPS is required, posts to `/api/server-auth/login`, and replace-navigates to `/` on success. |
| `app/unlock/page.tsx` | Dedicated unlock route (I18nProvider + form only). Redirects to `/` when auth is off; distinguishes blocked plaintext HTTP from explicitly allowed HTTP compatibility mode. |
| `components/AppDialogProvider.tsx` | Unified application dialog provider wrapping the app root. Renders a body portal for `alert`, `confirm`, and `prompt` dialogs with the shared `.pi-modal-*` shell, serial queue, trapped focus, Escape/Enter keyboard handling, overlay click cancel, semantic danger confirmation, auto-focused prompt input, focus restoration, and i18n-aware title/button labels. Exposes `useAppDialog()` hook returning `{ alert, confirm, prompt }` with Promise-based API. |
| `components/AppShell.tsx` | Top-level layout and sole source of truth for `activeCwd`. Concept-B workbench shell: three semantic-token card panels (left context / center chat / right Inspector). The center uses a focused session header: breadcrumb owns the shrinkable region; Appearance, Language, Web Terminal, Models, and Settings remain direct actions in that order; session context/cost/performance (`SessionResourcePanel`) plus ChatGPT/Grok quotas share one responsive resource surface; one Inspector trigger aggregates attention state; and export/system/Automation live in the overflow menu. Branches appear only when a real branch exists; the duplicate Observe row is retired because Changes / Git / Agents already live in Inspector tabs. At widths below 1200px the header becomes a stable two-row grid instead of horizontal scrolling. The right Inspector contains Changes (`InspectorChangesPanel`), Preview (file tabs + `FileViewer`), Git (`GitPanel`), SnFlow (`WorkflowPanel`), and Agents (`StoredSubagentPanel`); Alt+click on SnFlow still creates a task from chat. Preview reports active-file dirty state to the shell: file/tab/workspace or Inspector-mode switches require discard confirmation, and closing the Inspector keeps the active Preview mounted so edits are not silently reset. Owns URL/tab/language/Terminal/SnFlow/quick-command state, body-portaled auxiliary panels, the project quick-command composer bar + task-output dock (independent of Web Terminal), and right-panel width persistence under `localStorage["pi-web-right-panel-width-v2"]`. Consumes desktop-pet one-time deep-link intents once on mount (`inspector=snflow&task=`, `panel=automation`, `panel=quick-commands`) via `lib/desktop-deep-link.ts`, then strips those query keys while preserving `?session=`; closing a panel is not reopened by rerender. Settings-class surfaces (`ModelsConfig`, `SettingsConfig`, `UsageStatsModal`, `TerminalPanel`, `WorkflowPanel`, `AutomationPanel`) load through `next/dynamic` so the initial chat shell stays light; dynamic `loading` is `null` (no in-flow placeholder) and open controls prefetch the chunk on hover/focus. Automation unread while the drawer is closed is owned by `hooks/useAutomationUnread.ts` rather than keeping the heavy drawer mounted. Wide desktop (≥960px) docks the panel inline with pointer/keyboard resize and gutter-aware viewport clamping; narrow desktop (641–959px) uses an inset overlay drawer without a resize handle; ≤640px starts both Sidebar and Inspector closed, uses edge-to-edge overlay drawers, and keeps them mutually exclusive when either is opened. Workspace/session lifecycle invariants remain unchanged, including latest-ref deletion handling for WorkTree fallback and project switches. |
| `components/InspectorChangesPanel.tsx` | Embedded “Changes” view for the right Inspector tab. Fetches `/api/sessions/{id}/changes`, polls while the agent runs, refreshes on `refreshKey` bumps, and opens per-file diffs via `FileDiffModal`. Its stat cards, status badges, file rows, and empty/loading/error states use the shared Inspector visual primitives while preserving added/deleted domain meaning. |
| `components/ThemePicker.tsx` | Compact body-portaled appearance picker exposed as the first direct action in the focused header. Applies system, light, dark, curated professional skins, or opt-in MetaCubeXD-inspired Twilight, Night, Daisy Dark, and Dracula skins immediately through `useTheme`; also hosts Workbench skin controls (preset gradients, local wallpaper, glass, background blur, vignette, one-click reset) via `useWorkbenchSkin`. Preferences stay browser-local. |
| `components/SessionSidebar.tsx` | Controlled session sidebar shell (workspace cwd via `activeCwd` / `onActiveCwdChange` from AppShell). Owns business actions, top-level UI state, data-hook wiring, and composition of `components/sidebar/*`. The Workspace card, single primary New session action, search, day-grouped rows, collapsible archived rows, context menus, WorkTree confirmations, and Explorer consume shared semantic states. Workspace search is a separate mode over `/api/sessions/search` (not a client filter of the loaded page). Row actions and archive selection remain mounted and become visible on hover, `focus-within`, or coarse-pointer layouts, so keyboard and touch do not depend on hover. Archive/rename/delete/restore failures surface through `AppDialog.alert` and never optimistically remove rows on non-2xx. Session data remains in `hooks/useSessionBrowser` and fork rendering in `lib/sidebar-session-tree.ts`. |
| `components/sidebar/sidebar-utils.ts` | Pure cwd picker helpers/types: ordered cwds, shorten path, picker row build/group/filter, explorer height constants/storage. |
| `components/sidebar/WorkspacePicker.tsx` | Memoized workspace header, hover overflow menu (WorkTree / refresh / open project folder / archive all), project-switch modal entry, add-project directory picker entry, and WorkTree context menu. Clicking the workspace card opens `ProjectPickerDialog` (body portal) instead of an inline dropdown. Open-folder calls `POST /api/cwd/open` for the active cwd. Add-project first probes `GET /api/cwd/pick-native` and, when `preferNative`, opens the host OS folder chooser via `POST /api/cwd/pick-native`; on unavailable/error it falls back to `DirectoryPickerDialog` (user cancel does not fall back). Picker UI state is local; cwd/worktree actions are raised via props. |
| `components/sidebar/ProjectPickerDialog.tsx` | Body-portaled modal for switching the active project workspace. IDEA-style rows: colored initials avatar, bold project name, secondary full path, optional git-branch line for worktrees, search by name/path/branch, current-selection checkmark, default-directory and add-project footer actions, WorkTree context-menu hooks, focus trap, and Escape/overlay dismiss. Reuses shared `.pi-modal-*` shell and Settings controls. |
| `components/sidebar/DirectoryPickerDialog.tsx` | Body-portaled modal to browse server-side directories (`GET /api/cwd/browse`), jump via path input, and save through `POST /api/cwd/validate`. Uses browse `platform` for OS-specific subtitle/roots/placeholder copy. Optional “System picker” control re-enters the native chooser when local. Focus trap, Escape/overlay dismiss, and semantic Settings controls. Paths are always the WebUI server filesystem, not the browser client. |
| `components/sidebar/WorktreeBadge.tsx` | Memoized WorkTree badge chip shared by picker rows and session items. |
| `components/sidebar/SessionList.tsx` | Memoized active session tree/list with rename, multi-select archive bar, load-older pagination, and memoized leaf `SessionItem` / `SessionTreeItem` rows (rename focus + context menu preserved). Rename/delete HTTP failures call `onActionError` and do not remove rows until success. |
| `components/sidebar/SessionSearchResults.tsx` | Flat workspace search result list (active + archived badges, modified time, WorkTree chip, truncated/hasMore footer). Independent from recent pagination and fork-tree loading. |
| `components/sidebar/ArchivedSessionSection.tsx` | Memoized archived section expand/load-more and memoized muted archived leaf rows with unarchive/delete. Delete failures call `onActionError` and keep the row until success. |
| `components/sidebar/SidebarExplorerPane.tsx` | Memoized Explorer pane + desktop resize handle; height persisted under `localStorage` key `pi-web-explorer-height-v1`. At ≤640px the resize handle stays hidden and expanded Explorer shares remaining height with the session list. |
| `components/ChatWindow.tsx` | Memoized message list, SSE streaming, fork/navigate logic. Failure card localizes provider errors by stable code/category and offers Continue plus Open Models when auth/quota/model setup is the fix path. Same-session multi-tab read-only banner + takeover uses `useSessionTabLock`. Historical message rows and derived tool-result/stat projections retain stable identities during streaming, so token updates rerender only the live row instead of reparsing the full Markdown history. Row keys prefer persisted `entryIds`; optimistic/steer/follow-up rows without an entry id keep a WeakMap-backed local key for the message object lifetime, with a single keyed owner per row. Parent-shell subagent progress updates do not rerender the chat when its session props and callbacks are unchanged. Uses semantic chat state classes for loading/error/archive/drop-zone and the new-session hero while preserving the same session lifecycle. The empty new-session hero shows local web + pi versions and, via `/api/version-check`, a red update dot with hover tooltip when the public npm latest is newer. Renders the persistent agent failure card after exhausted retries, plays completion sound on `agent_settled`, and emits opt-in browser completion/failure notifications only while the page is hidden. Hosts Pi extension status chips, generic widget stacks around the composer, the floating todo panel, and body-portaled dialog/toast hosts for extension UI requests. Reports agent running state and extension todo-widget presence up to AppShell via `onAgentRunningChange`/`onTodoActiveChange` for the focused header's Inspector attention signal. |
| `components/ChatInput.tsx` | Semantic-token Composer for text, image/file attachments, model/thinking/tool controls, send/steer/follow-up/stop states, SnFlow task resume prompt injection, pre-send model readiness gate (block + Open Models CTA), multi-tab `writeLocked` gate, and bounded body-portaled dropdowns. When any model is marked `primaryCandidate` in Models, the model picker defaults to that short list with a top All row that opens a side flyout of the full list (hover/focus/click; Escape collapses the flyout first). The All flyout (and the full main list when no favorites exist) exposes per-model star toggles that PATCH `/api/model-favorites` immediately and update the chat short list without leaving the composer. Text plus uploaded-file references are saved as a bounded browser-local draft per existing/new session through `lib/chat-draft.ts`, restored after session switches, and removed after send; image blobs intentionally remain ephemeral. Upload failures render an actionable Composer alert instead of console-only diagnostics. Sound, auto-scroll, and opt-in browser notification controls live in the Composer row; notification permission is requested only by the user's click. Static control/menu/hover/focus visuals live in shared `chat-input-*` classes; only runtime popover coordinates, maximum heights, and editor height remain dynamic. Extension commands may show CLI-only / Partial badges from server `webSupport` metadata. |
| `components/ExtensionStatusBar.tsx` | Compact semantic-token chip row for Pi extension `setStatus` keys near the chat composer. |
| `components/ExtensionWidgetStack.tsx` | Multi-line semantic tool-surface cards for Pi extension `setWidget` content at `aboveEditor` / `belowEditor` placements; reserves the standard `todo-list` widget for the floating task panel. Chat also filters powerbar chrome and suppressed pi-subagents HUDs (`subagent-fleet-status`, `subagent-async`); the bridge in `lib/extension-web-ui.ts` drops the same keys before SSE so they never reach the client. |
| `components/ExtensionTodoPanel.tsx` | Floating session todo panel for the standard `todo-list` widget: draggable compact capsule entry with conic progress ring, expandable desktop panel, mobile bottom sheet, persisted/clamped position, semantic progress/active/complete states, and parsed task rows. Product copy uses i18n (`todoList` / active subtitle / all-done); technical widget source labels stay out of the chrome. Static visuals use `extension-todo-*`; only persisted position and progress width/CSS custom props remain inline. |
| `components/ExtensionDialogHost.tsx` | Body-portaled modal for blocking extension dialogs (`confirm`, `select`, `input`, `editor`) using the shared modal/form primitives, keyboard navigation, trapped/restored focus, and Escape cancel; replaces `window.confirm` / `window.prompt`. |
| `components/ExtensionToastHost.tsx` | Body-portaled semantic toast stack for non-blocking extension `notify` events (info/warning/error), with safe-area-aware mobile placement above the Composer; replaces `window.alert`. |
| `components/SessionResourcePanel.tsx` | Current-session resource trigger + body-portaled popover: compact context %, cost/token fallback, and exact weighted average TPS when samples exist. Popover shows average TPS/TTFT/sample count, mixed-model notice and provider/model rows, billing/context detail, accurate empty state (no historical backfill), and an explicit action to open the global Usage modal. Escape/outside focus closes and restores trigger focus. |
| `components/ChatGptUsagePanel.tsx` | Optional top-bar ChatGPT/Codex quota panel using the shared resource popover surface and semantic account/quota states. Its viewport-bounded body portal still avoids top-bar clipping, periodically revalidates cached active-account usage and reset credits, supports account activation, manual quota refresh/reset, and scheduler lock maintenance without changing API behavior. |
| `components/GrokUsagePanel.tsx` | Optional top-bar Grok subscription usage panel (gated by `pi-web.json.grok.usagePanelEnabled`) sharing ChatGPT’s trigger, popover, account-row, quota-card, auto-refresh status, lock repair, and narrow-viewport behavior. Compact trigger and detail body show only the weekly/`7d` pie. Opening the panel re-reads the shared usage cache and scheduler status so Models/backend refreshes are reflected without remounting the top bar. Account rows summarize cached `seven_day` tiers with the same `7d` labels as ChatGPT. Billing accepts OAuth from built-in xAI or `grok-cli`. |
| `components/ChatGptWarmupDialog.tsx` | Shared-shell ChatGPT/Codex warmup dialog with semantic notices, toggle, schedule/history surfaces, manual multi-select warmup, scheduled account/time settings, and recent run history. |
| `components/MessageView.tsx` | Memoized semantic renderer for user, assistant, Thinking, custom extension, tool-call, and tool-result messages. Assistant Markdown remains an open reading surface while nested Thinking/tool blocks own bordered cards; assistant `stopReason: "error"` / `errorMessage` render as an inline alert. Live streaming TPS remains a character-based estimate with an explicit `~` / estimated label and neutral styling (no fixed fast/slow color tiers). Action rows reveal through hover, `focus-within`, or coarse-pointer rules without event-driven color mutation. Stable historical props still skip rerenders while the live assistant message streams. |
| `components/BranchNavigator.tsx` | In-session branch switcher; its inline top-bar panel uses a fixed body portal so mobile toolbar overflow does not clip the branch tree. |
| `components/ChatMinimap.tsx` | Scroll minimap beside message list. |
| `components/ToolPanel.tsx` | Tool presets and preset inference helpers. |
| `components/ModelsConfig.tsx` | Shared resource-split modal shell for editing `models.json` (nav tree, selection, save). Detail domains live under `components/models/`: `types`, `form-fields`, `api-headers`, `provider-icons`, `discovery-pricing`, `ProviderModelDetail` (provider/model editors + discovery/pricing match), `OAuthDetail` (login/accounts/quota/Grok usage/import), `ApiKeyDetail` (secrets + DeepSeek balance), and `AddProviderPicker` (+ pricing sync status). It preserves Settings/resource primitives, default header seeding, OAuth/account import/activation, Grok/ChatGPT usage, pi.dev pricing sync/matching, original model-config save payload, custom provider ↑/↓ reorder by `providers` insertion order, per-provider expand/collapse, xAI/Grok per-account weekly usage pies, and WebUI-only `emptyCompletedRetry`. Favorites are a separate immediate-save boundary backed by `model-favorites.json`: authenticated built-in/subscription providers expose their available runtime models as default-collapsed expandable star rows, custom model tree/detail stars use the same API, and the chat picker defaults to the unified favorite set. |
| `components/GitPanel.tsx` | Git status surface showing branch, previewable/selectable commit graph by selected local branch, selected-commit metadata and changed files, staged/unstaged changes, untracked files, stash, and explicit branch switching. Commit and staged/unstaged file rows are keyboard/touch-accessible single-click controls that open bounded read-only diffs; mutation actions such as stage/discard remain intentionally out of scope. It reuses Inspector sections/rows/states and container-width responsive stacking; Git lane and change colors remain domain data. |
| `components/CommitGraph.tsx` | Git commit graph renderer with lane visualization, refs, semantic hover/selected/focus surfaces, bounded theme-aware tooltips, and optional selected-commit callbacks for the Git panel. Lane colors remain visualization data rather than generic UI status colors. |
| `components/GitCommitDiffModal.tsx` | Git commit file-diff adapter that fetches one selected commit file diff, formats commit/file metadata and fallback labels, and renders the shared diff modal. |
| `components/GitWorkingTreeDiffModal.tsx` | Staged/unstaged file-diff adapter that fetches one bounded read-only working-tree diff and renders it through the shared diff modal. |
| `components/SkillsConfig.tsx` | Shared resource-split panel for installed-skill browsing, model-invocation toggling, skills.sh search, scope selection, installation progress, and semantic empty/error states. Renders embedded inside the Settings modal Skills section (`embed` prop, bounded by `.skills-embed-body`) and can still render as a standalone modal. List rows are keyboard-focusable and stack above detail at mobile widths. |
| `components/ExtensionsConfig.tsx` | Shared resource modal with Resources/Settings tabs, count cards, source badges, diagnostics, filtered extension settings, dirty/save states, and semantic empty/error/loading views. It preserves `/api/pi/resources` discovery and `settings-extensions.json` patch behavior and can still render embedded in Settings. |
| `components/SettingsConfig.tsx` | Shared-shell Settings modal for interface language (`zh`/`en`), WorkTree defaults, Usage scan scope (with the ChatGPT usage panel / backend auto-refresh settings and the Grok usage panel toggle folded in as subsections), Vision fallback enablement plus an image-capable model selector, Web Terminal enablement/shell/env settings including Unix and Windows shell choices plus raw/AI env parsing model controls, Editor implementation/shortcut settings, native Pi subagent model settings (`AgentsConfig`), MCP adapter-native configuration (`McpConfig`), installed-skill management (embedded `SkillsConfig`), Web Search provider configuration (`WebToolsConfig`), per-bundle core-extension toggles, and SnFlow preferences (include-archived default) plus project inspection/init/update. Dirty Web-config edits require confirmation before modal close or page unload. Settings, Agents, MCP, Skills, Web Search, and embedded Extensions share presentation primitives while retaining independent save boundaries. Legacy `pi-web.json.trellis` data is left untouched by settings saves. ChatGPT warmup schedule is managed from `ChatGptWarmupDialog` and preserved by settings saves. |
| `components/AgentsConfig.tsx` | Native pi-subagents model configuration section in the Settings modal. Owns its independent load/save/dirty/error boundary, supports user-global and project scopes, loads/saves the `subagents` section of Pi `settings.json`, and uses shared Settings fields, badges, notices, actions, and responsive rows for source/model/thinking/fallback controls. Inherit, clear, legacy `false`, and user-global precedence semantics remain explicit text states. |
| `components/McpConfig.tsx` | Independent MCP settings panel in the Settings modal. Owns its independent load/save/dirty/error/conflict boundary (not part of the `pi-web.json` save payload) while reusing shared Settings fields, badges, notices, actions, and responsive grids. Edits adapter-native targets (user/project shared + Pi overrides) through `/api/mcp/config`, shows precedence diagnostics, server CRUD for stdio/HTTP/socket transports, explicit masked secret preserve/replace/clear controls, package configured vs runtime-unknown messaging, and `/reload` activation guidance. |
| `components/WebToolsConfig.tsx` | Independent Web Search settings panel for bundled `rpiv-web-tools`. Separates the persisted default search provider from the provider whose API key or SearXNG/Ollama URL is being configured, applies preserve/replace/clear operations through `/api/web-tools/config`, shows environment precedence and malformed/revision errors, and never stores an existing secret value in browser state. |
| `components/SubagentPanel.tsx` | Top-bar/Inspector subagent activity panel. Shared Inspector rows, status badges, detail cards, empty/error/loading states, and narrow-container wrapping sit above the unchanged abortable fingerprint-aware 32-entry detail cache, 2-second active refresh, depth-3 child loading, and truncation reporting. |
| `components/SubagentObservation.tsx` | Isolated external-store consumers: badge subscribes only to cached running/completed/failed counts; the full panel subscribes to run summaries only while the panel is mounted/open. |
| `components/WorkflowPanel.tsx` | WebUI-owned SnFlow drawer: per-project initialize/update states, create/select tasks under `.pi/snflows/tasks/` (including create-from-chat), edit requirements/design/plan Markdown, dispatch exact selected-task implement/check instructions, poll runs, record commit metadata, complete, and archive. Honors one-time `focusedTaskId` deep links and shows an unavailable fallback when the task is missing after list load. Task/run rows, badges, notices, and states share the Inspector language; a container-width breakpoint stacks task list over detail at narrow Inspector widths without changing lifecycle behavior. |
| `components/WorkflowSessionWidget.tsx` | Floating session-scoped SnFlow task capsule (plan/execute/finish), shown only when the selected non-archived chat has an exact pointer binding or explicit transcript evidence for the task; opens the SnFlow drawer focused on that task. |
| `components/SessionChangesFloatingPanel.tsx` | Floating chat-session file-change panel: draggable capsule entry aligned with the Todo overlay, expandable popover with semantic status badges/metrics, Escape-to-close, i18n copy, and per-file diffs. Static visuals use `session-changes-*`; only persisted position remains inline. |
| `components/FileDiffModal.tsx` | Session changed-file diff adapter that fetches one tracked file diff, formats session metadata and fallback labels, and renders the shared diff modal. |
| `components/DiffModal.tsx` | Shared read-only diff modal shell with close-on-Escape, overlay click close, source-specific header slot, shared loading/error/fallback states, and Unified/Side-by-side controls. Supports global and Inspector-contained placement; mobile becomes edge-to-edge while preserving both session and Git adapters. |
| `components/DiffView.tsx` | Shared diff renderer switch that renders unified diffs through `UnifiedDiffView` or parsed side-by-side diffs through `SideBySideDiffView`. |
| `components/SideBySideDiffView.tsx` | Theme-aware side-by-side unified diff parser/renderer with old/new line numbers, aligned modification rows, and metadata/hunk rows. |
| `components/UnifiedDiffView.tsx` | Theme-aware unified diff renderer for added, removed, hunk, header, and context lines. |
| `components/TerminalPanel.tsx` | Bottom-dock Web Terminal workspace using xterm; manages ephemeral multi-tab terminal sessions, per-pane tab strips, tab renaming, nested drag-to-split panes, pane and dock resizing, minimize/restore, app-local fullscreen, and destructive close confirmation while reusing existing terminal session APIs per tab. It is ordinary Web Terminal UI, not an extension-specific tool bridge. |
| `components/QuickCommandBar.tsx` | Compact composer-top entry for project quick commands: count badge, menu of name/description/command summary, manage entry, latest result chip (status/duration/view/rerun), and live-run affordances. Empty projects open the config modal instead of an empty menu. |
| `components/QuickCommandConfigModal.tsx` | Project quick-command management UI (portal dialog): edit name/command/description/relative cwd/env overrides/flags, revision-checked save to `.pi/quick-commands.json`. |
| `components/QuickCommandOutputPanel.tsx` | Dedicated bottom task-output dock (visually distinct from Web Terminal) using xterm for ANSI logs; run tabs, collapse-without-kill, stop, rerun, clear finished view, exit code and elapsed time. Surfaces desktop deep-link unavailable state when a run id is gone (e.g. after service restart). Closing the panel never cancels the process. |
| `components/QuickCommandTrustDialog.tsx` | First-run/changed/always-confirm trust dialog showing full command, resolved cwd, and env key names only. |
| `hooks/useQuickCommands.ts` | Client orchestration for quick-command list/config/run/SSE, trust confirmation, panel open/collapse state, composer result chips, and `openRunById` desktop deep-link entry (404 → panel unavailable fallback). Scoped to `activeCwd` project isolation. |
| `components/BrowserBindingTrigger.tsx` | Composer-row Browser button next to image/file attachments. Reuses Composer control classes, owns `useBrowserBridgeStatus` polling (15s idle / 4s while open), shows the domain status dot, and opens a body-portaled binding popover. |
| `components/BrowserBindingPanel.tsx` | Session-level browser control UI inside the composer popover: enable/pair extension, attach the current real-session binding intent to a newly issued pairing code, create later pending tab bind requests, list primary/additional bindings, toggle read-only debug, and revoke. First-time setup becomes WebUI code issuance → one explicit extension **Pair & connect current tab** click; already-paired/later tabs retain the explicit WebUI request + extension bind flow. When opened from the trigger it reuses shared status props (no second poller); standalone use enables its own poller only while expanded. Uses `/api/browser/*` only (not chat SSE). |
| `components/ModelPricingCatalog.tsx` | Searchable, provider-filterable read-only shared-shell modal for the locally cached pi.dev model pricing catalog, including context-window and token-price columns. |
| `components/UsageStatsModal.tsx` | Token/cost usage statistics modal with shared modal/form shell, 7/30/90 presets plus draft/Apply custom range, sequence-guarded stale-while-refresh loading, active/archive scan counts, parent-attributed native subagent costs, main/subagent splits, and scan source/duration/timezone meta. Requests `/api/usage?timeline=auto` and keeps the last successful payload on refresh failure. Defaults to current-cwd scope when a workspace is active. Dynamically imported from AppShell. |
| `components/UsageTokenChart.tsx` | Dependency-free HTML/CSS Usage timeline chart shared by Token structure (stacked input/output/cache-read/cache-write, absolute/100% modes, legend highlight) and single-series cost bars. Metric toggle stays client-side; roving keyboard focus, pointer/touch pin Tooltip, and viewport-clamped detail are shared. Consumes server timeline buckets only; metric/mode/legend interactions never fetch. |
| `components/FileExplorer.tsx` | On-demand file tree inside the sidebar; root loads are abortable and workspace changes clear old roots before applying the new response. Root and expanded-directory failures render explicit localized retry states rather than appearing empty. A row click keeps the existing right-panel preview, while row actions open the file in the standalone page or insert its relative path into chat. |
| `components/FileViewer.tsx` | Shared file content viewer/editor used by right-panel tabs and the standalone page; routes media/document previews, orchestrates text editing state, accepts an initial source line, and exposes Java implementation lookup results. Text editing reports dirty state to an optional shell callback and installs a page-unload guard while unsaved. Preview metadata bars, actions, live state, results, and error/loading surfaces use semantic classes and remain horizontally scrollable at minimum Inspector width. |
| `components/StandaloneFileViewer.tsx` | Full-viewport shell for `/file?path=...`; provides workspace navigation/path copy chrome and reuses `FileViewer`. Root-level historical Windows links such as `/D:/repo/File.java:11` redirect here through `app/[...legacyFilePath]/page.tsx`. |
| `components/MonacoFileEditor.tsx` | Monaco-backed source editor for text files, including language mapping, basic completions, word wrap, theme, and line-selection callbacks. |
| `components/FileIcons.tsx` | Monochrome SVG icons for files/folders. |
| `components/MarkdownBody.tsx` | Markdown, KaTeX, Mermaid, and syntax highlighting renderer. Streaming and collapsed large fenced blocks use lightweight `<pre><code>` (no Prism); settled expanded blocks keep full highlighting. Content-equal `memo` skips settled rerenders. Large blocks (≥80 lines or ≥4000 chars) default collapsed with copy retained. Mermaid still delays preview until streaming ends. Recognized loopback/absolute Windows file links are normalized through `lib/file-viewer-url.ts` and opened in the standalone file page; normal web links are unchanged. |
| `components/TabBar.tsx` | Secondary open-file tab bar beneath the Inspector’s primary tabs, with semantic selected/focus/close states, keyboard activation, bounded labels, and horizontal overflow. |

| `components/ui/SettingsPrimitives.tsx` | Minimal presentation-only settings primitives: section/header, associated field label, native input/select/textarea wrappers, accessible switch, button variants, semantic badge/notice, responsive action row, surface, segmented tabs, and loading/empty/error state. These components own no fetch, save, dirty, conflict, secret, or resource-specific logic. |

## Hooks

| File | Purpose |
| --- | --- |
| `hooks/useSessionBrowser.ts` | Sidebar session browser data layer: project summaries, mtime-cursor paged active sessions (`loadMore`), paged archived sessions, abort/cwd isolation, outside-window selected-session patches via `GET /api/sessions/[id]`, and independent debounced workspace search (`searchQuery` → `/api/sessions/search` with AbortController + cwd/seq isolation). Search results never merge into `projectSessions`. Load-more/archive list failures surface through `error` / `onError`. |
| `hooks/useAgentSession.ts` | Central chat/session hook: data loading, SSE, streaming state (including the pre-model Vision resolution phase), commands, tools, model metadata, thinking levels, and bounded subagent summary orchestration. Prompt-level running state settles on `agent_settled` (not bare `agent_end`); `willRetry` / `auto_retry_*` keep the composer busy through backoff. Surfaces `retryInfo` during automatic retries and a persistent `agentFailure` card after exhausted retries or `prompt_error`. Streaming prefers in-process cumulative `event.message`, with fallback to `assistantMessageEvent.partial` for 0.84+ wire-compatible payloads. Hidden tabs coalesce cumulative `message_update` snapshots to 500 ms while flushing before lifecycle events/when visible; top-bar lifetime token/cost totals come from session-detail `sessionStats` (with current-context fallback only before a persisted session is available); optional `sessionPerformance` loads from the same detail payload and accepts live `session_performance_update` SSE events only when the event session id matches the active session. Live progress normalize/match/flush pure helpers live in `lib/subagent-progress.ts` (re-exported for compatibility); extension dialog/toast/status/widget chrome lives in `hooks/useExtensionUi.ts`. Ordinary server progress no longer carries output; compatibility/final/persisted output is capped to an 8k preview while full bounded detail loads on expansion. Summary state stays outside React chat state and flushes at 300 ms, with terminal/attention updates immediate. The AppShell callback writes an external store rather than top-level React state. Optional browser metrics use `NEXT_PUBLIC_PI_WEB_SUBAGENT_OBSERVABILITY=1`. |
| `hooks/useExtensionUi.ts` | Extension UI chrome bridge for the active session: dialog / toast / status / widget maps, `extension_ui_request` handling, dialog responses via `sendAgentCommand`, and cleanup on session teardown. Consumed only by `useAgentSession`. |
| `hooks/useTheme.ts` | Browser-local appearance store for system/light/dark and curated skins, including MetaCubeXD-inspired Twilight, Night, Daisy Dark, and Dracula skins, with OS color-scheme updates, reduced-motion-aware view-transition animation, and a resolved `isDark` compatibility projection for Monaco/Markdown consumers. |
| `hooks/useWorkbenchSkin.ts` | Browser-local Workbench skin store for background mode (`none` / `image` / `gradient`), optional wallpaper data-URL, preset gradient id, and 0–100 glass / background-blur / vignette. Compresses local images client-side, persists under `pi-theme-*` keys, and applies `data-wallpaper` / `data-glass` plus `--skin-*` tokens without leaving the semantic theme layer. |
| `hooks/useDragDrop.ts` | Drag-and-drop image attachment handler. |
| `hooks/useAudio.ts` | Sound toggle and completion chime playback. |
| `hooks/useCompletionNotification.ts` | Browser-local, explicit opt-in completion notification state. Requests permission only from the Composer control, detects denied/unsupported states, and emits tagged notifications only while the page is hidden. |
| `hooks/useAutoScroll.ts` | Persisted chat auto-stick-to-bottom preference used by the message list and input toggle. |
| `hooks/useSessionTabLock.ts` | Same-session multi-tab write lock via `BroadcastChannel`: elected/claimed tab keeps write; other tabs are read-only with an explicit Take over control. Wired by `ChatWindow` into composer send/steer/fork gates. |
| `hooks/useAutomationUnread.ts` | Lightweight Automation inbox poller for the AppShell badge while the drawer is closed. Fetches only `/api/automations/inbox`, applies persisted read ids, and pauses when the full `AutomationPanel` owns unread updates. |
| `hooks/useBrowserBridgeStatus.ts` | Shared browser-bridge status poller for the composer Browser control. Dedupes in-flight fetches, slows idle polling to 15s, and speeds up to 4s while the popover is active so Trigger + Panel do not double-hit `/api/browser/status`. |

## Styles

Global CSS lives in `app/globals.css`. New or migrated UI must consume semantic roles rather than palette values or component-specific colors:

```text
Surface:   --surface-app --surface-panel --surface-raised --surface-subtle
           --surface-hover --surface-selected --surface-overlay
Border:    --border-default --border-subtle --border-strong --border-focus
Text:      --text-primary --text-secondary --text-tertiary --text-inverse --text-accent
Accent:    --accent-primary --accent-hover --accent-soft --accent-border
Status:    --status-{success|warning|danger|info}-{foreground|soft|border}
Shape:     --radius-* --control-height-*
Elevation: --shadow-sm --shadow-panel --shadow-popover
Focus:     --focus-ring-color --focus-ring-width --focus-ring-offset
Motion:    --motion-duration-* --motion-ease-*
```

Theme selectors set only `--palette-*` values; the root semantic layer maps those values to component roles. Existing variables remain compatibility aliases during gradual migration: `--bg*` → `--surface-*`, `--border`/`--line*` → `--border-*`, `--text`/`--text-muted`/`--text-dim` and `--text-2`/`--text-3` → semantic text roles, `--accent` → `--accent-primary`, `--ok`/`--warn`/`--danger`/`--info` → semantic status foregrounds, and `--radius*` → semantic radius roles. Do not remove an alias until repository search shows that it has no callers. Tailwind `--color-*` aliases resolve through the same semantic layer.

`lib/theme.ts` is the single theme registry. Each entry owns its resolved mode, skin flag, i18n label key, and three-color Picker preview; `ThemePicker`, `useTheme`, and the pre-hydration script in `app/layout.tsx` derive their options/mode/skin behavior from this metadata. The appearance system stores the selected preference under `localStorage["pi-theme"]`, toggles `dark` on `document.documentElement` for binary editor/renderer compatibility, and sets `data-theme-skin` only for curated skins. Invalid stored values fall back to `system`, and applying a core theme clears stale skin data before first paint. The opt-in MetaCubeXD family (`twilight`, `night`, `daisy-dark`, and `dracula`) retains its existing scoped material effects; Night, Daisy Dark, and Dracula preserve the DaisyUI 5.7.0 base/content/primary relationships without adding DaisyUI as a dependency.

Workbench skin extras live in `lib/theme-skin.ts` and stack on top of the curated preference. Background mode is `none` | `image` | `gradient` (`localStorage["pi-theme-bg-mode"]`), with optional local wallpaper data-URL (`pi-theme-wallpaper`), preset gradient id (`pi-theme-gradient`), and independent 0–100 levels for glass (`pi-theme-glass`), frost clarity (`pi-theme-frost-clarity`, scales the glass-derived panel blur down to zero), background blur (`pi-theme-bg-blur`), and vignette (`pi-theme-vignette`). The boot script restores the full stack before paint via `buildWorkbenchSkinBootFragment()`. CSS uses a fixed `body::before` blur layer and `body::after` scrim/vignette so text stays sharp; tokens are `--skin-bg-image` / `--skin-wallpaper-image`, `--skin-glass`, `--skin-panel-alpha`, `--skin-panel-blur` (alias `--skin-blur`), `--skin-bg-blur`, `--skin-vignette`, and `--skin-scrim`, plus `data-wallpaper=image|gradient` and `data-glass`. Wallpaper never leaves the browser, remote URLs are rejected, and high-frequency reading surfaces keep solid semantic fills. First background enable defaults glass/blur/vignette to moderate values when still off; Reset clears extras without changing the curated theme. `prefers-reduced-motion` disables background blur.

Run `npm run test:ui-theme` whenever changing the theme registry, boot script, Picker metadata, semantic/layer Token names, compatibility aliases, stable shared UI classes, responsive breakpoints, Portal focus behavior, coarse-pointer/safe-area rules, or reduced-motion behavior. The smoke checks synchronization and static contracts; it does not replace the representative-theme browser matrix in `docs/operations/ui-visual-validation.md`.

Chat visuals use three scoped class families in `app/globals.css`: `message-*` for message/action/Thinking/tool hierarchy, `chat-input-*` for the Composer and viewport-bound menus, and `extension-*` for status/widget/todo/toast surfaces. Assistant prose must remain an open reading surface rather than receiving the same bordered card as nested tools. Static hover/focus/disabled/status visuals belong in these classes; inline styles are limited to runtime coordinates, measured heights, drag positions, progress widths, and domain visualization values. Message actions must remain reachable through `focus-within` and coarse-pointer rules, not hover alone.

Inspector content uses shared `inspector-*` primitives for sections, stat cards, list rows, badges, scrolling, and empty/loading/error states. Preview adds the quieter `preview-tab-*` level; Git/Diff use `git-*`, `commit-graph-*`, and `diff-*`; SnFlow and Agents use `workflow-*` and `subagent-*`. Generic surfaces, borders, focus, and statuses must use semantic Tokens, while commit lanes and added/deleted diff data retain domain color meaning. Narrow behavior follows container width—not only viewport width—so the 300px Inspector stacks SnFlow list/detail and Git history/detail without page-level horizontal overflow. Inline values remain valid for graph geometry, drag positions, and recursive indentation.

Settings surfaces use `components/ui/SettingsPrimitives.tsx` plus `settings-*` classes for ordinary fields, toggles, buttons, badges, notices, action rows, surfaces, segmented tabs, and state views. Use `SettingsField` for associated label/description/error content, `SettingsBadge` for compact textual classification, `SettingsNotice` for actionable status or risk, `SettingsActionRow` for wrapping header/footer actions, `SettingsSurface` for nested groups, `SettingsTabs`/`SettingsTab` for compact local views, and `SettingsButton` variants for action hierarchy. The primitives are presentation-only: owning components retain loading, validation, dirty, save, secret, conflict, and API behavior. MCP, Agents, Models, Extensions, and Skills keep their existing independent data/save boundaries. Blocking dialogs and simple modals use one `.pi-modal-overlay` / `.pi-modal-panel` / header/body/footer shell; panel size and runtime content remain component-specific. Dense configuration UIs share `resource-*`, `models-*`, and `usage-*` layout classes; generic surface, input, button, focus, and status appearance must stay in semantic Settings/resource primitives, while quota pies and provider logos may retain domain visualization colors. At ≤640px Settings navigation scrolls horizontally, resource/model split navigation stacks above detail, wide grids become one column, and shared modal panels fill the viewport.

Desktop split sizing uses shared `.panel-resize-handle` styles plus `--right-panel-width` on `.app-shell-root`. Inline resize applies at `min-width: 960px`, with a 300px Sidebar, 360px minimum Chat target, and 300px minimum / 380px baseline Inspector. The shell gutter scales from 0px at 960px to 12px on wider viewports; `AppShell` subtracts the same two paddings and two gaps when clamping a stored Inspector width. Between `641px` and `959px` the Inspector is an inset overlay drawer with no resize handle. At `max-width: 640px`, Sidebar and Inspector are full-height overlay drawers, all resize handles are hidden, expanded Explorer equal-shares the remaining Sidebar height, and Sidebar/Inspector/Composer/Toast/Todo surfaces honor safe-area insets. The Inspector starts collapsed on first load in every layout (`rightPanelOpen` defaults to `false`); users reopen the last active tab through the focused header's Inspector trigger, and narrow layouts also force it closed on breakpoint transitions. The retired 860px media query must not be reintroduced as a layout state; `npm run test:ui-theme` statically checks these breakpoint contracts.

Shared keyboard behavior is part of the visual contract. Theme Picker and Inspector tabs use roving focus with arrow keys plus Home/End; Composer Model/Thinking/Tools portals expose listbox/option semantics, move focus into the active option, close on outside focus, and return focus to the trigger on Escape/selection. Application and extension dialogs trap focus, ignore disabled controls, and restore the previously focused element. At coarse-pointer breakpoints, high-frequency top-bar, Composer, Inspector, Settings, and dialog actions receive enlarged targets. The final global `prefers-reduced-motion` gate suppresses decorative transitions/animations while status text, icons, and shapes remain meaningful.

Portal stacking must use the `--z-*` layer Tokens in `app/globals.css`, not new component-local large z-index values. The order is workbench card → drawer backdrop → Sidebar/Inspector drawers → Automation drawer → top portals → terminal fullscreen → context menu → dialogs. Keep runtime coordinates and measured sizes inline, but keep surface, focus, motion, touch-target, and layer behavior in stable semantic classes.

## Internationalization (i18n)

- **Supported locales**: `zh` (default when browser is Chinese), `en`.
- **Provider**: `components/I18nProvider.tsx` wraps the app in `app/page.tsx`.
- **Catalogs**: `lib/i18n/messages/*` nested dictionaries; lookup via dotted keys such as `sidebar.archiveAllSessions`.
- **API**: `const { locale, setLocale, t } = useI18n()`; `t("key", { name })` interpolates `{name}` placeholders.
- **Persistence**: `localStorage["pi-locale"]`; boot script in `app/layout.tsx` sets `document.documentElement.lang` early to reduce flash.
- **Switcher**: top-bar `EN`/`中` button in `AppShell`, and Settings → Language section.
- **Coverage status**: Wave A–C complete for product chrome. Catalogs live under `lib/i18n/messages/*` (including `errors.ts`). App locale drives selected date/number formatting via `lib/i18n/format.ts`. Stable error codes (`lib/i18n/error-codes.ts`) cover OAuth import / Grok not-logged-in / terminal env assist pilots. Key parity check: `npx tsx scripts/check-i18n-keys.ts` (also `npm run test:i18n`). Residual: ModelsConfig secondary strings, broader API error codes, type-safe `t()` keys.

## Desktop pet UI (Electron companion)

Separate from the Next.js WebUI. Lives under `desktop/` and is packaged independently of npm `spi`.

| File | Purpose |
| --- | --- |
| `desktop/renderer/index.html` | Transparent pet shell + Activity tray markup; hover/focus shows a compact top-left list (`#pet-intent-menu`) for 全部已读 / 快速会话 / 设置 without covering the snail or changing collapsed window size; compact primary-activity context meter and the “显示上下文占比” setting live next to the pet/settings chrome; the “勿扰模式” toggle in the settings panel feeds the narrow preload `setPrefs` bridge; U4a adds the “声音” settings section (master switch + 待输入/完成 event switches + “声音已被勿扰模式静音” DND note); in-tray “快速会话” composer (`#quick-session-panel`) stays mutually exclusive with settings/activity list; pet picker groups built-in / Snail / Codex with a filter box; strict CSP (`default-src 'none'`, `img-src 'self' data: blob:`, `connect-src 'none'`, no `media-src` — sounds are synthesized). |
| `desktop/renderer/pet.css` | Transparent CSS/vector snail personalities, state posture/animation, renderer-only Running cue modifiers, compact static context ring + readable percent, tray/settings/quick-session layout that keeps the same expanded window size (tray stretches to the window; settings and quick session fill the same remaining height as the activity list), small/medium/large scale tokens, reduced-motion gate, and non-color cues. U3 adds low-amplitude `pet-sleepy`/`pet-sleeping` postures (half-closed/closed eyes, slow breathing, drooped antennae) plus a `z`/`Z` glyph overlay that doubles as the spritesheet sleep cue. U4b adds short, interruptible `reaction-poke`/`reaction-flail` wrapper-transform nudges (durations match `PET_POKE_ANIMATION_MS`/`PET_FLAIL_ANIMATION_MS`; spritesheet pets reuse existing frames, no manifest change). |
| `desktop/renderer/pet-state.ts` | Pure presentation helpers (bundled manifest frames, glyphs/labels, localized source labels, local-allowlist Running cue classification + dwell/debounce reducer, primary-activity context meter from `sessionResources.context.percent`, verifiable progress formatting, local activity filters/counts, filtered-list selection/reset, transition/revision-driven bubble reducer with DND gating — task bubbles are silenced without replay while connection diagnostics stay visible, elapsed). U3 adds renderer-local progressive idle sleep: `resolveIdleSleepStage`/`nextIdleSleepBoundaryMs`/`shouldRunProgressiveSleep` + `reduceIdleSleepState`, with `PET_SLEEPY_AFTER_MS`/`PET_SLEEPING_AFTER_MS` thresholds and the pointer-wake throttle constant. The three packaged v2 documents are validated before use; invalid documents fall back to CSS snails. U4b adds the click-sequence reducer `reducePetClickSequence` (+ `createInitialPetClickSequenceState`), `resolvePetReaction`, `shouldAllowPetReaction`, and concentrated timing constants `PET_DOUBLE_CLICK_INTERVAL_MS`/`PET_QUAD_CLICK_WINDOW_MS`/`PET_POKE_ANIMATION_MS`/`PET_FLAIL_ANIMATION_MS`. U6 slice 1 adds the runtime custom manifest registry (`registerCustomPetManifest`/`clearCustomPetManifests`/`getCustomPetManifest`), combined `getPetManifest` resolution (builtin → custom → first builtin fallback), and `resolveCustomPetManifest` (spritesheet-only custom manifest gate). |
| `desktop/renderer/pet-assets.ts` | Manifest v2 allowlist/path/size/state contract used by renderer fallback, build, and package smokes. Spritesheet descriptors require bounded start/count/timing/static-frame ranges inside declared sheet capacity and single-row contiguous frame runs (no row wrap); source builds and expanded package artifacts verify required files and byte limits. Custom-pet gates stay Snail-specific. Catalog rows and lazy bitmaps are re-validated by `validateRendererCatalogEntry` / `validateRendererPetAsset` (no paths). |
| `desktop/renderer/pet-sheet.ts` | Pure spritesheet runtime math: cell→background-position/size, uniform `steps()` or uneven `step-end` keyframes (never `linear` between cells), reduced-motion static frame, and injectable stylesheets. `resolveSpriteStylesheetSource` keeps lazy custom/Codex blob URLs on the profile builder. Profile-driven CSS keys off `data-clip` so Codex look/drag overlays do not rewrite business `data-state`. |
| `desktop/renderer/pet-sheet-assets.ts` | Built-in sprite sheets inlined as `data:` URLs by esbuild. Custom/Codex sheets use Blob URLs registered by catalog key (`setCustomPetSheetUrl` / `revokeOwnedSheetUrl`). |
| `desktop/renderer/pet-sound.ts` | U4a local Web Audio cue player. Receives only the finite `attention | completion` vocabulary; plays fixed short (<300ms) two-tone patterns at a hard-capped gain with no files/network/queue; missing/suspended/throwing AudioContext fails silently; `destroy()` closes the context on renderer teardown. |
| `desktop/renderer/quick-session-state.ts` | Pure composer reducer for compact project/model pickers, in-memory draft, requestId reuse on result-unknown retry, and cancel/success cleanup. |
| `desktop/renderer/pet-app.tsx` (+ generated `pet-app.js`) | Pet click toggles tray; a presentation/active/updatedAt selector—not project-name ordering—drives the pet/caption/contextual action and the compact context ring; Running reuses its existing CSS/spritesheet frame with a debounced finite cue glyph/label; the pet-side ring uses only the selected primary Agent `context.percent` and hides on non-Agent, missing/`null`/invalid percent, stale snapshot, or the settings toggle; Activity tray rows keep their existing TPS/cost/Token text; transition-keyed status bubbles keep attention/Ready visible but time-bound Running/Retrying without replay, and manual DND (view `dndEnabled`) closes/silences task bubbles immediately without touching pet state or tray unread; renderer-local All/Attention/Running/Completed filters; task rows show source/phase/real progress, expandable safe Subagent summaries, and separate Open task / Mark read actions; inline local pet/size/always-on-top/click-through/autostart/notification/context-meter/DND settings plus restore-default-position-and-size; settings/quick-session reuse the Activity tray chrome and keep the same width/height; service-not-running copy/retry. U4a: subscribes to the read-only `onSoundCue` bridge, plays cues through `PetSoundPlayer` (failure-silent), syncs the sound master/per-event switches + DND note, and tears the player down on destroy. Applies spritesheet pets via `pet-sheet` (inject style, gate decorative timers, decode-verify with CSS fallback). U3: a single renderer-local sleep timer advances awake → sleepy → sleeping while the observer stays idle; same-state snapshots never reset the idle origin, real states/hide/reduced-motion/press/drag reset it, and pointerdown/hover(keyboard/tray/settings) wake it without any business side effect. U4b: resolved pet clicks route through the pure click-sequence reducer so the first click still toggles the tray / 直达 immediately while a double/quad click fires one poke/flail without re-toggling the tray; `P`/`Shift+P` keyboard equivalents only when the pet is focused; hover/focus shows a compact 全部已读 / 快速会话 / 设置 list in the pet's top-left pocket (collapsed windows expand the tray first when opening settings or quick session); drag, pointercancel, lost capture, hidden, state preemption and destroy all reset the sequence/timers/classes; the fun reaction is decorative-only in idle and never the sole expression of a business state. Quick session: header entry opens an overlay composer mutually exclusive with settings/activity list; compact project/model chips open a floating dropdown that collapses on select or outside click; Enter inserts a newline, Ctrl/Cmd+Enter submits, IME composition does not submit; drafts stay in renderer memory until cancel/success. No token/Node. |
| `desktop/preload/pet-preload.ts` | Narrow `window.snailPet` bridge; U4a adds read-only `onSoundCue` subscription that allowlist-validates the cue kind before forwarding. |
| `desktop/renderer/pet-key.ts` | Namespaced `snail:<id>` / `codex:<id>` keys, CSS tokens, and legacy `selectedPetId` migration. |
| `desktop/renderer/codex-pet-assets.ts` | Codex `pet.json` parser for v1/v2 fixed atlases. Known display fields only; unknown/capability keys never enter the runtime profile. |
| `desktop/renderer/pet-runtime-profile.ts` | Shared clip profile: Snail manifest and Codex atlas both project here. Maps the 8 business states onto Codex actions and quantizes v2 look / drag overlays. |
| `desktop/assets/pets/*` | Builtin versioned pet manifests (`snail-default`, `snail-classic` CSS; `snail-sprite` spritesheet with `snail.png` generated by `scripts/generate-desktop-pet-sheet.mjs` as placeholder art). Custom Snail packs are generated by `skills/desktop-pet-assets`. Codex packs come from local hatch-pet output or unzipped directories; they are not shipped in the package. |

Smoke: `npm run test:desktop-contract`, packaging contract `npm run test:desktop-package`. Dev launch: `npm run desktop:build` / `npm run desktop:dev` (see `desktop/README.md`). Visual matrix: `npm run desktop:preview` and `docs/operations/desktop-pet-visual-review.md`. Release/QA matrix: `docs/operations/desktop-pet-validation.md`. UI: drag grip moves window; **×** hides to tray; settings can switch small/medium/large and restore the default dock. Snapshot/reconnect/notification updates never `show()`/`focus()` a hidden pet; tray click, “显示桌宠”, and second-instance may activate.

## Automation UI

| File | Purpose |
| --- | --- |
| `components/AutomationPanel.tsx` | Global Automation drawer: task list/detail/editor, runs, inbox. Accepts one-time `initialDeepLink` `{ taskId, runId }` from desktop pet URLs; missing runs show an unavailable fallback without reopening on rerender. |
| `components/AutomationTaskEditor.tsx` | Explicit Save draft / Review & activate editor. |
| `components/AutomationToolPicker.tsx` | Dimensional risk tool picker. |
| `components/AutomationRunList.tsx` | Run history list. |
| `components/AutomationRunViewer.tsx` | Read-only transcript/promote viewer (no RPC wrapper). |
| `components/AutomationInboxBadge.tsx` | Unread badge helper. |
| `hooks/useAutomations.ts` | Control session, polling, approval, lifecycle actions. |
| `lib/i18n/messages/automation.ts` | zh/en Automation copy. |
