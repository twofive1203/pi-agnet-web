{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "phase-b-secure-observer",
  "description": "Phase B pet-window capability; Observer HTTP/SSE and tokens stay in allowlisted Rust commands.",
  "windows": ["pet"],
  "permissions": ["core:default"]
}
