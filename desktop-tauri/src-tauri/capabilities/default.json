{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "phase-c-feature-parity",
  "description": "Phase C pet-window capability; privileged network, secrets, filesystem and opener stay in allowlisted Rust commands.",
  "windows": ["pet"],
  "permissions": ["core:default"]
}
